object model encomposes th epricipal of abstraction , encapsulation , modularity,heirarchy,typing concurrency and presistence.object model basically ephasizes on the object and the class. Main concept related with object and models are classes and their association with attributes.predefined relationships and objects model are aggregation and generalization 